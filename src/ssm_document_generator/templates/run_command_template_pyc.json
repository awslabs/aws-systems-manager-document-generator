{
  "schemaVersion": "2.2",
  "mainSteps": [
    {
      "action": "aws:runShellScript",
      "name": "runShellScript",
      "inputs": {
        "id": "0.runShellScript",
        "runCommand": [],
        "timeoutSeconds": "{{ executionTimeout }}"
      }
    }
  ]
}