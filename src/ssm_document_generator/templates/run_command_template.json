{
   "schemaVersion":"2.2",
   "description":"Base template for defining OWLS commands",
   "parameters": {
     "executionTimeout":{
            "type":"String",
            "default":"120",
            "description":"(Optional) The time in seconds for a command to complete before it is considered to have failed. Default is 120. Maximum is 28800 (8 hours).",
            "allowedPattern":"([1-9][0-9]{0,3})|(1[0-9]{1,4})|(2[0-7][0-9]{1,3})|(28[0-7][0-9]{1,2})|(28800)"
        }
   },
   "mainSteps":[
      {
         "action":"aws:runShellScript",
         "name":"runShellScript",
         "inputs":{
            "id":"0.runShellScript",
            "runCommand":[],
            "timeoutSeconds":"{{ executionTimeout }}"
         }
      }
   ]
}